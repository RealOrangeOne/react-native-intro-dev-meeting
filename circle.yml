machine:
  node:
    version: 5.6.0

dependencies:
  pre:
    - npm install
    - npm run build
  cache_directories:
    - node_modules

deployment:
  master:
    branch: master
    commands:
      - ./scripts/deploy.sh
